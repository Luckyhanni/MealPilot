{
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "install:all": "npm install --include=dev --prefix backend && npm install --include=dev --prefix frontend",
    "build": "npm install --include=dev --prefix backend && npm install --include=dev --prefix frontend && npm run recipes:enrich:missing --prefix backend && npm run recipes:build && npm run build --prefix frontend && npm run build --prefix backend",
    "start": "node backend/dist/startProduction.js",
    "dev": "concurrently -n BACKEND,FRONTEND -c green,blue \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "recipes:split": "node scripts/splitRecipes.mjs",
    "recipes:build": "node scripts/buildRecipesBundle.mjs",
    "recipes:classify": "node scripts/classifyMealPilotRecipes.mjs",
    "recipes:validate": "node scripts/validateRecipeCategories.mjs",
    "recipes:import:protein": "node scripts/importHelloFreshProteinCandidates.mjs"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
